/**
 * Previously showed a "demo mode" notice when Supabase env was missing.
 * Hidden so the public site reads as a normal product UI; local demo data still works.
 */
export default function SupabaseConfigBanner() {
  return null;
}
