# سجل نشاطات المستخدمين

## SQL
نفّذ في Supabase SQL Editor:

```sql
-- أو الملف كامل:
-- scripts/sql/020_activity_logs.sql
```

```bash
npm run db:apply-dds
```

## ما يُتتبّع تلقائياً
- تسجيل الدخول / الخروج
- تصفح الصفحات (`VIEW_PAGE`)
- إنشاء/تحديث العملاء والعروض والعقود
- أرشفة المستندات
- طباعة وتصدير المستندات

## الواجهة
الإعدادات ← سجل النشاطات (لمدير النظام / `canManageStaff` فقط)
